import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clinicflow.connect',
  appName: 'ClinicFlow Connect',
  webDir: 'dist'
};

export default config;
